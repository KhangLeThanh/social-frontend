export enum MenuPositions {
  TOP = "top",
  BOTTOM = "bottom",
  LEFT = "left",
  RIGHT = "right",
  CENTER = "center",
}

export enum StatusFriendRequest {
  Accepted = "accepted",
  Pending = "pending",
  Rejected = "rejected",
}
